#include <stdio.h>
#include<string.h>

int main() {
    char str1[50];
    printf("enter the string\n");
    scanf("%s", str1);
    int i;
    for (i = 0; str1[i]!='\0'; ++i)
    {
    
    }
   printf("%d",i);
return 0;
}